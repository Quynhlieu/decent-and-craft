export default interface Voucher{
    createdDate: string
    modifiedDate: string
    id: number
    voucherCode: string
    description: string
    quantity: number
    amount: number
    conditions: number
    expirationDate: string
}